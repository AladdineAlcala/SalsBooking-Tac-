<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header with-border">
                    <i class="fa fa-cart-plus fa-fw"></i>
                    <h3 class="box-title">Bookings List of Schedule</h3>
                </div>

                <div class="box-body">
                 

                    <table class="table table-condensed table-hover" id="tbl_eventsBooking" style="width:100%;">
                        <thead>
                        <tr>
                            <th></th>
                            <th>Customer</th>
                            <th>Event</th>
                            <th>Event Date/Time</th>
                            <th>Package</th>
                            <th>Booking Type</th>  
                            <th></th>
                        </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>

<div id="spinn-loader">
    Loading data Pls. wait....
</div>

@section scripts
{

    <script type="text/javascript">


        var bookingsUrl = {
            bookUrl_IndexLoad:"@Url.Action("Index", "Bookings")",
            bookUrl_loadBookings: "@Url.Action("LoadBookings", "Bookings")",
            bookUrl_createBooking: "@Url.Action("CreateBooking", "Bookings")",
            bookUrl_packageBooking: "@Url.Action("GetPackageonBooking","Bookings", new { transId = "trans_Id" })",
            bookUrl_getPackageBookingDetailsId:"@Url.Action("GetPackageBookingDetails", "Bookings",new {transId = "trans_Id"})",
            bookUrl_getPackageId: "@Url.Action("GetPackageId","Bookings")",
            bookUrl_editBooking: "@Url.Action("EditBooking", "Bookings",new {transId="tId"})",
            bookUrl_paymentBooking: "@Url.Action("GetBooking_Payments", "Payments",new {transactionId="tId"})",
            bookUrl_ServeBooking: "@Url.Action("ServeBookingStatus","Bookings")",
            bookUrl_removeBooking: "@Url.Action("RemoveBooking", "Bookings")",
            bookUrl_cancelBooking:"@Url.Action("CancelBooking", "Bookings",new {transId = "trans_Id"})"

        }
    </script>

    @Scripts.Render("~/Scripts/js_bookings.js")

}
